# Packaging

This folder is for building packages of Wordwise.

So far I only worked on the Linux .deb package.

## What is here

- `linux/` — files for building a Debian and Ubuntu .deb package

## Why

The main Wordwise project only has Android and iOS builds and I wanted to run it on Linux desktop and also have a package I can install so I added this.

